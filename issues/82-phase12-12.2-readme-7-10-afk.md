# 12.2 README §7–10

## What to build

Fill in README sections 7–10:

7. MF runtime manifest pattern — why we don't use static `remotes:` config; how the host fetches `/api/remotes`; LKG caching
8. `shared-store` ownership + versioning — single-source-of-truth slice/event ownership; the 1.0.0 pin + version-drift script
9. Cross-MFE comms split — Redux for shared state, event bus for ephemeral notifications; the 2 demo events (`product:added-to-cart`, `auth:logout`); "single fact, single emission" rule
10. Mock-auth disclaimer — what's mocked, what the NextAuth seam would look like in production

A reviewer should be able to answer "how do remotes register?" after reading §7.

Refs: `sepc.md` §24.

## Acceptance criteria

- [ ] Sections 7–10 filled
- [ ] §7 explains the manifest call + LKG cache + fallback behavior
- [ ] §8 documents the version-drift defense (config + script)
- [ ] §9 has a small diagram or list showing where Redux vs event bus belongs
- [ ] §10 explicitly names NextAuth as the production replacement

## Blocked by

- `81-phase12-12.1-readme-1-6-afk.md`

## User stories covered

- Dev #37
- Interviewer #39

## Status

Pending
