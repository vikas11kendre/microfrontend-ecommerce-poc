# 0.6 Add .gitignore + README skeleton

## What to build

Add a root `.gitignore` covering `node_modules`, `.next`, `.env.local`, build dist directories. Create a `README.md` skeleton with the 19 section headings from `sepc.md` §24 stubbed (content filled in Phase 12).

Refs: `sepc.md` §22, §24.

## Acceptance criteria

- [ ] `.gitignore` exists with entries for `node_modules`, `.next`, `.env.local`, `dist`, `coverage`, `playwright-report`
- [ ] `README.md` exists with 19 H2/H3 section headings from `sepc.md` §24 (content can be TBD placeholders)
- [ ] README renders the section headers cleanly in a GitHub preview

## Blocked by

- `01-phase0-0.1-init-monorepo-root-afk.md`

## User stories covered

- Dev #37 (README documents architectural decisions)

## Status

Completed
