# 12.3 README §11–14

## What to build

Fill in README sections 11–14:

11. API integration — fakestoreapi base URL + endpoint table; client-side search filter rationale
12. Search/filter/pagination patterns — URL-as-state; debounce; infinite vs load-more toggle
13. Testing strategy — Vitest for slices + event bus + bootstrapRemotes + ErrorBoundary; one Playwright E2E for the system; what's intentionally NOT tested
14. **Failure-modes matrix** — three tiers (manifest / page-remote / widget-remote), with recovery story for each. Include the "navbar never breaks" guarantee.

§14 is the most important — interviewer #41 explicitly wants this depth.

Refs: `sepc.md` §24.

## Acceptance criteria

- [ ] Sections 11–14 filled
- [ ] §14 contains a 3-row table: manifest-tier (LKG cache + WarningBanner), page-tier (ErrorBoundary + RemoteError + retry), widget-tier (ErrorBoundary fallback=null)
- [ ] §14 names the "navbar never breaks" guarantee and explains why (navbar lives outside any ErrorBoundary)

## Blocked by

- `82-phase12-12.2-readme-7-10-afk.md`

## User stories covered

- Interviewer #41 (failure-modes matrix)

## Status

Pending
